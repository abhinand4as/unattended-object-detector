"""Unattended-object detection: closed-set person detection + open-vocabulary
luggage detection + BoxMOT tracking + ownership/abandonment alerts.

See detector.main for the CLI entry point (also installed as the
`unattended-object-detector` console script) and ARCHITECTURE.md at the
project root for how the pipeline fits together.
"""

__version__ = "0.1.0"
